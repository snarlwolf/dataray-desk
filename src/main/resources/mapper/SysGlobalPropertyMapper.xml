<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skydawn.desk.core.mapper.SysGlobalPropertyMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.skydawn.desk.core.entity.SysGlobalProperty">
        <id column="id" property="id"/>
        <result column="system_name" property="systemName"/>
        <result column="category" property="category"/>
        <result column="prop_key" property="propKey"/>
        <result column="prop_value" property="propValue"/>
        <result column="is_cipher" property="isCipher"/>
        <result column="remark" property="remark"/>
        <result column="created_time" property="createdTime"/>
        <result column="created_by" property="createdBy"/>
        <result column="updated_time" property="updatedTime"/>
        <result column="updated_by" property="updatedBy"/>
    </resultMap>

    <!-- 查询所有全局属性配置（仅 belong_sys 属于指定列表，供本系统使用） -->
    <select id="selectAllForApp" resultMap="BaseResultMap">
        SELECT 
            id, 
            system_name, 
            category, 
            prop_key, 
            prop_value, 
            is_cipher, 
            remark, 
            created_time, 
            created_by, 
            updated_time, 
            updated_by 
        FROM sys_global_property
        WHERE belong_sys IN
        <foreach collection="belongSysList" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
        ORDER BY system_name, category, prop_key
    </select>

    <!-- 查询所有全局属性配置（不过滤 belong_sys） -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT 
            id, 
            system_name, 
            category, 
            prop_key, 
            prop_value, 
            is_cipher, 
            remark, 
            created_time, 
            created_by, 
            updated_time, 
            updated_by 
        FROM sys_global_property
        ORDER BY system_name, category, prop_key
    </select>

    <!-- 根据条件查询全局属性配置 -->
    <select id="selectByKey" resultMap="BaseResultMap">
        SELECT 
            id, 
            system_name, 
            category, 
            prop_key, 
            prop_value, 
            is_cipher, 
            remark, 
            created_time, 
            created_by, 
            updated_time, 
            updated_by 
        FROM sys_global_property
        WHERE system_name = #{systemName}
          AND category = #{category}
          AND prop_key = #{propKey}
        LIMIT 1
    </select>

    <!-- 根据系统名称和分类查询全局属性配置列表 -->
    <select id="selectBySystemAndCategory" resultMap="BaseResultMap">
        SELECT 
            id, 
            system_name, 
            category, 
            prop_key, 
            prop_value, 
            is_cipher, 
            remark, 
            created_time, 
            created_by, 
            updated_time, 
            updated_by 
        FROM sys_global_property
        WHERE system_name = #{systemName}
          AND category = #{category}
        ORDER BY prop_key
    </select>

    <!-- 根据系统名称和分类查询（仅 belong_sys 属于指定列表，用于 property-version / hub-version） -->
    <select id="selectBySystemAndCategoryForApp" resultMap="BaseResultMap">
        SELECT 
            id, 
            system_name, 
            category, 
            prop_key, 
            prop_value, 
            is_cipher, 
            remark, 
            created_time, 
            created_by, 
            updated_time, 
            updated_by 
        FROM sys_global_property
        WHERE system_name = #{systemName}
          AND category = #{category}
          AND belong_sys IN
        <foreach collection="belongSysList" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
        ORDER BY prop_key
    </select>

    <!-- 更新版本号（仅更新 belong_sys 属于指定列表的行） -->
    <update id="updateVersion">
        UPDATE sys_global_property
        SET prop_value = #{propValue},
            updated_time = NOW(),
            updated_by = #{updatedBy}
        WHERE system_name = #{systemName}
          AND category = #{category}
          AND prop_key = #{propKey}
          AND belong_sys IN
        <foreach collection="belongSysList" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </update>

</mapper>
